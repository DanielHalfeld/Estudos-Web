function executar() {
    console.log('Executar')
}

const obj = document.getElementById('entrada')
//obj.addEventListener('keydown', executar) -> pressionar qualquer tecla
//obj.addEventListener('keyup', executar) -> apenas executa quando solta a tecla
//obj.addEventListener('keypress', executar) -> executa apenas 1 pressionamento, mesmo se segurar